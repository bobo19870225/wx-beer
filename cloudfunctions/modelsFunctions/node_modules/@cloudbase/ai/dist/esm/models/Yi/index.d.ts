import { type ParsedEvent } from 'eventsource-parser';
import type { YiGenerateTextOutput, YiStreamTextOutput } from './type';
import type { ChatModel, StreamTextResult, ModelReq, BaseChatModelInput } from '../../type';
export declare class YiModel implements ChatModel {
    private req;
    baseUrl: string;
    subUrl: string;
    constructor(req: ModelReq, baseUrl: string, subUrl?: string);
    private get url();
    generateText(data: BaseChatModelInput): Promise<{
        text: string;
        rawResponse: YiGenerateTextOutput;
    }>;
    streamText(data: BaseChatModelInput): Promise<YiModelStreamResult>;
}
declare class YiModelStreamResult implements StreamTextResult {
    private _eventSourceStream;
    constructor(_stream: ReadableStream<Uint8Array>);
    private get teeedStream();
    get eventSourceStream(): import("../../type").AsyncIterableReadableStream<ParsedEvent>;
    get dataStream(): import("../../type").AsyncIterableReadableStream<YiStreamTextOutput>;
    get textStream(): import("../../type").AsyncIterableReadableStream<string>;
}
export {};
