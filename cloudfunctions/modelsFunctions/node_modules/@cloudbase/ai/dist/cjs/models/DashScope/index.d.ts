import { type ParsedEvent } from 'eventsource-parser';
import type { DashScopeInputData, DashScopeGenerateTextOutput, DashScopeStreamTextOutput } from './type';
import type { ChatModel, StreamTextResult, ModelReq } from '../../type';
export declare class DashScopeModel implements ChatModel {
    private req;
    baseUrl: string;
    subUrl: string;
    constructor(req: ModelReq, baseUrl: string, subUrl?: string);
    private get url();
    generateText(data: DashScopeInputData): Promise<{
        text: string;
        rawResponse: DashScopeGenerateTextOutput;
    }>;
    streamText(data: DashScopeInputData): Promise<DashScopeModelStreamResult>;
}
declare class DashScopeModelStreamResult implements StreamTextResult {
    private _eventSourceStream;
    constructor(_stream: ReadableStream<Uint8Array>);
    private get teeedStream();
    get eventSourceStream(): import("../../type").AsyncIterableReadableStream<ParsedEvent>;
    get dataStream(): import("../../type").AsyncIterableReadableStream<DashScopeStreamTextOutput>;
    get textStream(): import("../../type").AsyncIterableReadableStream<string>;
}
export {};
